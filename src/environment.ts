export class Environment {

    // Archivos dentro de la carpeta /public
    // no hace falta especificar que vienen de la carpeta public
    // ya que Vite al hacer el bundle los deja juntos
    public static readonly PROLOG_ENGINE_PUBLIC_FILE_PATH = "prolog-engine.js"
    public static readonly PROLOG_PROGRAM_PUBLIC_FILE_PATH = "prolog-program.pl"

}